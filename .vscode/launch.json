{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Load Balancer",
            "program": "${workspaceFolder}/loadbalancer.js"
        }
    ]
}